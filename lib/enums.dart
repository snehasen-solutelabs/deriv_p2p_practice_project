enum AdvertType {
  buy,
  sell,
}

enum APIErrorType {
  serverError,
  restrictedCountry,
  disableAccount,
  unwelcomeAccount,
  expiredAccount,
  unsupportedCurrency,
  notAvailableCountry,
  accountNotLoaded,
  unknownError,
}
